import Component from "@glimmer/component";
import { tracked } from "@glimmer/tracking";

export default class AddonsEmberSimpleSetHelperComponent extends Component {
  @tracked isExpanded = false;
}
